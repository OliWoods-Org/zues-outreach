import { ModuleStub } from './ModuleStub';

/** Route shell until Content Studio is wired to Briefings / templates / Airtable. */
export function ContentStudio() {
  return (
    <ModuleStub
      eyebrow="Content"
      title="Content Studio"
      description="Ideate, draft, and approve posts with brand voice — connects to Briefings Brain and publish queue per docs/ZEUS_FINAL_BUILD_PLAN.md."
    />
  );
}
